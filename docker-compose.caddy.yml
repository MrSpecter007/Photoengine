services:
  web:
    container_name: photo-app
    ports: []
    # catalystdev-local joined manually after startup to avoid DB hostname collision:
    #   docker network connect catalystdev-local photo-app

  nginx:
    profiles:
      - disabled
